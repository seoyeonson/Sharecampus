// 미리보기 이미지 
    // const file = document.querySelector("input[type='file']");

    // file.addEventListener("change", function(e){
    //     var reader = new FileReader();
    //     reader.readAsDataURL(e.target.files[0]);
    //     reader.onload = function(e){
    //         let url = e.target.result;

    //         if(url.includes("image")){
    //             document.querySelector("label div").style.backgroundImage = "url(" + url +")";
    //         }else{
    //             document.querySelector("label div").style.backgroundImage = "url(img/no_image.jpg)";
    //         }
    //     }
    // });

    
/*
현재 보여지는 DB에서의 생일 innerHtml 값
#now_Birth

DB로 보낼 생일의 value값
#chan_Birth

현재 보여지는 DB에서의 성별 innerHTML 값
#now_Gender

DB로 보낼 성별의 value값
#chan_Gender.values

원래있는 이메일의 placehoder 값
#email_view630
입력받는 이메일의 value값
#email_view630

비밀번호 변경 창으로 바뀌는 버튼
#chan_password

아이디 연동을 위한 창으로 이동
#first_id_connect_navar
// naver
#first_id_connect_kakao
// kakao
.documet_file[type='file']

*/