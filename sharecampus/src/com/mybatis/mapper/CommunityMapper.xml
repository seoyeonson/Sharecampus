<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="community">
<!-- 해당 멤버 넘버로 커뮤니티 정보조회 -->
	<select id="select" parameterType="_int" resultType="communityDTO">
		select community_num, community_regist_date, community_contents, member_id, m.member_num
		from tbl_member m join tbl_community b
		on m.meber_number = b.member_number = #{boardNumber} and m.member_num = b.member_num
	</select>
<!-- 해당 테이블 삭제  -->
	<delete id="delete" parameterType="_int">
		delete community_num, community_regist_date, community_contents, member_id from tbl_member
		where community_num = #{boardNumber}
	</delete>


</mapper>