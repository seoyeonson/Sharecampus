<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="community">
	<!-- 해당 테이블 추가 -->
		<insert id="insert" parameterType="communityVO">
			insert into tbl_community (member_num, community_contents)
			values(#{memberNum}, #{communityContents})
		</insert>
	<!-- 테이블 개수 확인 -->
		<select id="getTotal" resultType="_int">
			select count(community_number) from tbl_community
		</select>
	<!-- 해당 테이블 정보 조회하기 -->
		<select id="selectAll" parameterType="map" resultType="boardDTO">
		select board_number, board_title, board_content, board_date, board_read_count, m.member_number, member_id
		from tbl_community m join tbl_board b
		on m.member_number = b.member_number
		order by board_number desc
	</select>
	
	create table tbl_community_comment(
comm_comment_num int not null auto_increment primary key,
member_num int unsigned,
community_num int not null,
comm_comment_contents varchar(500) not null,
comm_comment_date datetime default now(),
	
</mapper>